# KaspaConcert API – Environment variables
# Copy to .env and adjust.

PORT=4000
NODE_ENV=development

# Allowed frontend origin (for CORS). Use comma-separated list for multiple.
CORS_ORIGIN=http://localhost:3000

# Frontend base URL (for shareable session links in API response).
FRONTEND_ORIGIN=http://localhost:3000

# Kaspa API (optional). When set, transaction monitor uses real confirmation by polling for the transaction.
# RECOMMENDED (no API key, free): https://api.kaspa.org — public Kaspa REST server.
# Alternative (when signup works): https://api.kas.fyi + KASPA_API_KEY from developer.kas.fyi (signup currently down).
# KASPA_RPC_URL=https://api.kaspa.org
# KASPA_API_KEY=optional-only-for-api-kas-fyi

# Mock confirmation delay in ms (used when KASPA_RPC_URL is not set).
MOCK_CONFIRM_DELAY_MS=2500
